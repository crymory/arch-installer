# 📥 Как скачать installer через `curl` (правильно и безопасно)

> Короткая и понятная инструкция для GitHub README

---

## 1️⃣ Размести скрипт на GitHub

Допустим, у тебя есть репозиторий:

```
https://github.com/crymory/arch-installer/
```

Файл лежит в корне репозитория:

```
installarch.sh
```

⚠️ **Важно:** нужен именно **RAW-файл**, а не обычная страница GitHub.

---

## 2️⃣ Скачать файл через `curl`

```bash
curl -LO https://raw.githubusercontent.com/crymory/arch-installer/main/installarch.sh
```

### Что означают флаги:

* `-L` — следовать HTTP-редиректам
* `-O` — сохранить файл с оригинальным именем

---

## 3️⃣ Проверить, что именно скачалось (ОБЯЗАТЕЛЬНО)

```bash
ls -lh installarch.sh
head -n 20 installarch.sh
```

❗ **Никогда не запускай скрипт, не посмотрев его содержимое.**

---

## 4️⃣ Сделать файл исполняемым

```bash
chmod +x installarch.sh
```

---

## 5️⃣ Запуск

🚨 **Запускать ТОЛЬКО из Arch Linux ISO**

```bash
sudo ./installarch.sh
```

---

## 🔐 Более безопасный вариант (рекомендуется)

```bash
curl -fsSL https://raw.githubusercontent.com/crymory/arch-installer/main/installarch.sh -o installarch.sh \
  && nano installarch.sh \
  && chmod +x installarch.sh \
  && sudo ./installarch.sh
```

### Используемые флаги `curl`:

* `-f` — ошибка, если файл не найден (404)
* `-s` — тихий режим
* `-S` — показать ошибку, если она есть
* `-L` — следовать редиректам

---

## ✅ Рекомендации

* Всегда проверяй скрипты перед запуском
* Не запускай installer из-под установленной системы
* Используй `nano` / `less` / `bat` для просмотра

---

🧠 **Безопасность важнее удобства.**
